# Skill: <content type>  (rename this folder to the content type)

Make ONE piece of content for this set, then post it to every account in `accounts/`
via that account's API. Made once — never remade per platform.

## Produces
<!-- the one piece this skill makes, e.g. a short-form video -->

## Reads
- `company/` — product, voice, ICP (shared)

## Steps
1. Make the content once → `output/`.
2. Post: for each account file in the set's `accounts/`, publish the piece (adapted to
   the platform) using that account's handle + API key.

## Output
The content lives once in `output/`, then posts to the set's `accounts/`. Each account
file is just a handle + API key — the posting destination, not a content folder.
