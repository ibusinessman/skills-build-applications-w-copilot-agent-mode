# account — instagram

- handle: <@handle>
- api_key: IG_ACCESS_TOKEN
- disabled: true

<!-- Instagram Graph API does not support text-only posts — every post requires an image
     or video URL. This account is disabled for organic-text runs. To enable it, the
     skill must generate an image (e.g. a quote card) and pass it as --media.
     Remove `disabled: true` once the skill produces an image. -->
